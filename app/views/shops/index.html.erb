<div class="container">
  <div class="shop_card_selection">
    <% @shops.each do |shop| %>
      <%= link_to shop_path(shop) do %>
        <%= render "shared/shop_card", shop: shop %>
      <% end %>
    <% end %>
  </div>
</div>
